import { Component } from "solid-js";
import { Section } from "../components";

export const Settings: Component<{}> = (props) => {
  
  return (
    <Section title="Settings" class="bg-slate-700">
      blacklist and stuff
    </Section>
  )
};